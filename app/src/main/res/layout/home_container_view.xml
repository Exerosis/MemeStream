<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="view"
            type="stream.meme.app.home.HomeContainerView" />
    </data>


    <android.support.v4.widget.DrawerLayout
        android:id="@+id/drawerLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:activity="@{view}"
        app:contentDescriptionClosed="@{@string/description_close}"
        app:contentDescriptionOpen="@{@string/description_open}"
        app:startOpen="@{controller.DrawerOpen}"
        app:toolbar="@{@id/toolbar}">

        <android.support.design.widget.CoordinatorLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fitsSystemWindows="true">

            <android.support.design.widget.AppBarLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:theme="@style/AppTheme.AppBarOverlay">

                <android.support.v7.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?actionBarSize"
                    app:homeAsUp="@{true}"
                    app:layout_scrollFlags="enterAlways"
                    app:popupTheme="@style/AppTheme.PopupOverlay"
                    app:supportActionBar="@{(AppCompatActivity) controller.getActivity()}" />
            </android.support.design.widget.AppBarLayout>

            <com.bluelinelabs.conductor.ChangeHandlerFrameLayout
                android:id="@+id/container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:layout_behavior="@string/appbar_scrolling_view_behavior" />
        </android.support.design.widget.CoordinatorLayout>


        <android.support.design.widget.NavigationView
            android:id="@+id/navigationView"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            app:controller="@{controller}"
            app:headerLayout="@{@layout/home_header_view}"
            app:menu="@menu/home_navigation_menu"
            app:navigationItemSelectedListener="@{controller}" />

    </android.support.v4.widget.DrawerLayout>


</layout>
